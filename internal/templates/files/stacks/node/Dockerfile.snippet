# stack: node — Node.js (pinned Node 24 LTS) is already installed in every OS base
# image, so selecting the node stack adds nothing further. Kept as a selectable
# stack for discoverability and back-compat.
